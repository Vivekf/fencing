"""Analytics pipeline (Phase E): the dynamic low-rank fencing rating model.

Builds a calibration-ready per-bout design matrix from `fencing.db` and (later) fits
the model described in `analytics.tex`.
"""
